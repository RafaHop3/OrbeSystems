# Terraform state files — NEVER commit these
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Secret variables file — NEVER commit
terraform.tfvars

# Lambda deployment package — generated by deploy.sh
lambda.zip

# Crash logs
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
