{
  "InstanceIds": ["i-058e26140671b3254"],
  "DocumentName": "AWS-RunShellScript",
  "Parameters": {
    "commands": [
      "cd /home/ubuntu/OrbeSystems",
      "docker rm -f orbe_redis orbe_postgres orbe_backend inho_backend 2>/dev/null; docker network rm orbesystems_default 2>/dev/null; docker volume rm orbesystems_postgres_data 2>/dev/null",
      "docker compose up -d --build"
    ]
  }
}